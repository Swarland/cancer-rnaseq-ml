[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cancer_ml"
version = "0.1.0"
description = "Cancer RNA-seq machine learning project"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["src"]